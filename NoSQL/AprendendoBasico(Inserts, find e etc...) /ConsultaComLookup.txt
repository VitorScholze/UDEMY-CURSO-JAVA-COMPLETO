db.empresas.aggregate([
    {$match: {nome: "Bradesco"}},
    {$lookup: {
        from:"estados", 
        localField: "estadosId", 
        foreignField: "_id",
        as : "estado"
    }},
    {$unwind: "$estados"}, 
])

db.empresas.aggregate([
    {$match: {nome: "Vale"}},
    {$lookup: {
        from:"estados",
        localField: "cidadeId",
        foreingField: "cidades._id",
        as: "estado"
    }}
]).pretty()



